import React from 'react';
import Navbar from '../navbar/Navbar';

const Header = () => {
    return (
        <>
            <Navbar />
            {/* Other header components if any */}
        </>
    );
}

export default Header;
